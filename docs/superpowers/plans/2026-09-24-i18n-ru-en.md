# Quakpit i18n (English + Russian) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Двуязычный интерфейс (английский + русский) для всего приложения: ядро i18n в `src/shared/i18n/`, переводы всех ~160 видимых строк, переключатель языка в настройках с автоопределением языка системы, русское README с описанием изменений форка.

**Architecture:** Собственный микро-i18n без зависимостей: плоские TS-каталоги `en.ts`/`ru.ts` (en — источник типов), ядро `createI18n`/`resolveLocale`/`tPlural` без импорта Electron (юнит-тестируется). Main-процесс локализует строки в месте создания через обёртку `src/main/i18n.ts`; окно настроек размечается атрибутами `data-i18n*` и апликатором DOM. Новый язык = новый каталог-файл.

**Tech Stack:** TypeScript (strict), electron-vite (3 таргета), vitest, без новых зависимостей.

**Spec:** `docs/superpowers/specs/2026-09-24-i18n-ru-en-design.md` — план спорит со спекой; исполнитель читает оба документа.

## Global Constraints

- Новые npm-зависимости запрещены. Ничего не добавляем в `package.json` `dependencies`/`devDependencies` (кроме правки `description`).
- Приватность — обещание продукта: не добавлять логирование/персистентность событий календаря, не добавлять сетевые вызовы.
- Все видимые пользователю строки — только из каталогов i18n; захардкоженного UI-текста не остаётся. Комментарии в коде — на английском.
- TypeScript strict: `npx tsc --noEmit` обязателен к концу каждой задачи. CI гоняет только `tsc` + `build` — **тесты CI не запускает**, `npm test` прогонять локально в каждой задаче.
- Имена брендов не переводятся: Quakpit, Ooble Studio, Apple iCloud, Microsoft Exchange, Google Calendar, macOS, Dock, EWS.
- Event ids могут содержать `:` (например `ical:abc`) — в `scheduler.ts` не трогать логику префиксного матчинга `fired`-ключей.
- **`src/main/calendar/google.ts`: НЕ менять строки 105–112 и 243–248** (там обработка OAuth client secret). Заменяются только перечисленные строки ошибок.
- `site/` и фон DMG (`build/dmg-background.png`) — вне области, не трогать.
- Рендереры собираются с `root: 'src/renderer'`; импорт `'../../shared/i18n'` из рендереров — ожидаемый и рабочий путь (Vite разрешает импорты за пределами root в пределах проекта).

## Review Focus

Пять классов входных данных, которые тесты задач не покрывают полностью — проверять вручную:

1. **Плюрализация ru на грчных значениях** (11–14, 21, 101, 111) — ожидается форма «многих»; закреплено тестом в Задаче 1 (`pluralIndex`).
2. **Потеря плейсхолдеров при переводе** (`{title}`, `{minutes}`, `{n}`, `{status}`) — баннер покажет «{minutes}» literal; закреплено тестом в Задаче 2 (`catalogs.test.ts`, сравнение токенов en/ru).
3. **Старый `prefs.json` без `lang` и с пользовательским английским `messageTemplate`** — ожидание: шаблон не перезаписывается, UI локализуется, баннер летит с сохранённым шаблоном; закреплено шагом ручной проверки в Задаче 3 и порядком merge в `getPrefs` (дефолты — подложка, raw — поверх).
4. **Смена языка на лету** — трей, окно настроек, свотчи, статусы календарей и следующий пролёт баннера должны переключиться без перезапуска; закреплено чек-листом ручного smoke в Задаче 8.
5. **`textContent` в `applyI18n` стирает вложенные SVG/разметку** — если перевесить `data-i18n` на элемент с `<svg>` внутри, иконки исчезнут; закреплено кодом `i18n-apply.ts` и обёртками `<span>` в Задаче 6 (проверка: `npm run build` + визуальный осмотр в smoke).

---

### Task 1: Ядро i18n (`src/shared/i18n/core.ts`)

**Files:**
- Create: `src/shared/i18n/core.ts`
- Create: `src/shared/i18n/index.ts`
- Create: `src/shared/i18n/en.ts` (пустой стаб)
- Create: `src/shared/i18n/ru.ts` (пустой стаб)
- Create: `src/shared/i18n/plurals.ts` (пустой стаб)
- Test: `src/shared/i18n/i18n.test.ts`

**Interfaces:**
- Consumes: ничего (файл чистый, без Electron — юнит-тестируется в vitest).
- Produces (используется всеми следующими задачами):
  - `type Locale = 'en' | 'ru'`, `type LangPref = 'auto' | Locale`, `type Params = Record<string, string | number>`, `type Dict = Record<string, string>`
  - `type Catalogs = Record<Locale, Dict>`, `type PluralForms = { en: [string, string]; ru: [string, string, string] }`, `type PluralTable = Record<string, PluralForms>`
  - `resolveLocale(pref: LangPref | undefined, system: string): Locale`
  - `pluralIndex(locale: Locale, n: number): number`
  - `createI18n(locale: Locale, catalogs: Catalogs, plurals: PluralTable): I18n` где `I18n = { locale; t(key, params?); tPlural(key, n, params?) }`
  - Из `index.ts`: связанная `createI18n(locale: Locale): I18n` (с полными каталогами) + реэкспорт типов и `resolveLocale`.

- [ ] **Step 1: Write the failing test**

Создать `src/shared/i18n/i18n.test.ts`:

```ts
import { describe, expect, it } from 'vitest'
import { createI18n, pluralIndex, resolveLocale, type Catalogs, type PluralTable } from './core'

const catalogs: Catalogs = {
  en: { 'greet.hello': 'Hello {name}!', 'only.en': 'English only' },
  ru: { 'greet.hello': 'Привет, {name}!' }
}
const plurals: PluralTable = {
  'test.count': {
    en: ['{n} item', '{n} items'],
    ru: ['{n} предмет', '{n} предмета', '{n} предметов']
  }
}

describe('resolveLocale', () => {
  it('returns explicit preferences', () => {
    expect(resolveLocale('ru', 'en-US')).toBe('ru')
    expect(resolveLocale('en', 'ru-RU')).toBe('en')
  })
  it('auto-detects Russian system locales', () => {
    expect(resolveLocale('auto', 'ru-RU')).toBe('ru')
    expect(resolveLocale('auto', 'ru')).toBe('ru')
  })
  it('falls back to English for other/unknown locales', () => {
    expect(resolveLocale('auto', 'en-US')).toBe('en')
    expect(resolveLocale('auto', 'de-DE')).toBe('en')
    expect(resolveLocale(undefined, 'en-US')).toBe('en')
  })
})

describe('pluralIndex', () => {
  it('english: one vs other', () => {
    expect(pluralIndex('en', 1)).toBe(0)
    expect(pluralIndex('en', 2)).toBe(1)
    expect(pluralIndex('en', 0)).toBe(1)
  })
  it('russian: one/few/many with 11-14 edge cases', () => {
    for (const n of [1, 21, 101]) expect(pluralIndex('ru', n)).toBe(0)
    for (const n of [2, 3, 4, 22, 24]) expect(pluralIndex('ru', n)).toBe(1)
    for (const n of [0, 5, 11, 12, 14, 20, 25, 111]) expect(pluralIndex('ru', n)).toBe(2)
  })
})

describe('t', () => {
  it('interpolates params', () => {
    expect(createI18n('en', catalogs, plurals).t('greet.hello', { name: 'Jack' })).toBe('Hello Jack!')
    expect(createI18n('ru', catalogs, plurals).t('greet.hello', { name: 'Даша' })).toBe('Привет, Даша!')
  })
  it('falls back to English, then to the key itself', () => {
    expect(createI18n('ru', catalogs, plurals).t('only.en')).toBe('English only')
    expect(createI18n('ru', catalogs, plurals).t('nope.missing')).toBe('nope.missing')
  })
  it('keeps unknown placeholders as-is', () => {
    expect(createI18n('en', catalogs, plurals).t('greet.hello', { other: 'x' })).toBe('Hello {name}!')
  })
})

describe('tPlural', () => {
  it('picks the right russian form and interpolates {n}', () => {
    const ru = createI18n('ru', catalogs, plurals)
    expect(ru.tPlural('test.count', 1)).toBe('1 предмет')
    expect(ru.tPlural('test.count', 3)).toBe('3 предмета')
    expect(ru.tPlural('test.count', 11)).toBe('11 предметов')
    expect(ru.tPlural('test.count', 21)).toBe('21 предмет')
  })
  it('picks the right english form', () => {
    const en = createI18n('en', catalogs, plurals)
    expect(en.tPlural('test.count', 1)).toBe('1 item')
    expect(en.tPlural('test.count', 5)).toBe('5 items')
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/shared/i18n/i18n.test.ts`
Expected: FAIL — «Cannot find module './core'» (или эквивалентная ошибка разрешения).

- [ ] **Step 3: Write minimal implementation — `src/shared/i18n/core.ts`**

```ts
// Minimal i18n core shared by the main process and both renderers.
// NO Electron imports here — this file must stay unit-testable in plain vitest.

export type Locale = 'en' | 'ru'
export type LangPref = 'auto' | Locale
export type Params = Record<string, string | number>
export type Dict = Record<string, string>
export type Catalogs = Record<Locale, Dict>
export type PluralForms = { en: [string, string]; ru: [string, string, string] }
export type PluralTable = Record<string, PluralForms>

export type I18n = {
  locale: Locale
  t: (key: string, params?: Params) => string
  tPlural: (key: string, n: number, params?: Params) => string
}

/** Resolves the effective locale from a user preference and the system locale. */
export function resolveLocale(pref: LangPref | undefined, system: string): Locale {
  if (pref === 'ru' || pref === 'en') return pref
  return system.toLowerCase().startsWith('ru') ? 'ru' : 'en'
}

/** Picks the plural form index: en = [one, other]; ru = [one, few, many]. */
export function pluralIndex(locale: Locale, n: number): number {
  if (locale === 'en') return n === 1 ? 0 : 1
  const mod100 = Math.abs(Math.trunc(n)) % 100
  const mod10 = mod100 % 10
  if (mod10 === 1 && mod100 !== 11) return 0
  if (mod10 >= 2 && mod10 <= 4 && !(mod100 >= 12 && mod100 <= 14)) return 1
  return 2
}

function interpolate(template: string, params: Params | undefined): string {
  if (!params) return template
  return template.replace(/\{(\w+)\}/g, (match, name: string) =>
    Object.prototype.hasOwnProperty.call(params, name) ? String(params[name]) : match
  )
}

/** Creates a translator bound to a locale. Missing keys fall back to en, then to the key. */
export function createI18n(locale: Locale, catalogs: Catalogs, plurals: PluralTable): I18n {
  const dict = catalogs[locale] ?? catalogs.en
  const t = (key: string, params?: Params): string =>
    interpolate(dict[key] ?? catalogs.en[key] ?? key, params)
  const tPlural = (key: string, n: number, params?: Params): string => {
    const forms = plurals[key]
    const form = forms ? forms[locale][pluralIndex(locale, n)] : (dict[key] ?? catalogs.en[key] ?? key)
    return interpolate(form, { ...params, n })
  }
  return { locale, t, tPlural }
}
```

- [ ] **Step 4: Create stubs and the public facade — `src/shared/i18n/en.ts`, `ru.ts`, `plurals.ts`, `index.ts`**

`src/shared/i18n/en.ts`:
```ts
import type { Dict } from './core'

/** English source strings (filled in Task 2). en is the source of truth. */
export const en: Dict = {}
```

`src/shared/i18n/ru.ts`:
```ts
import type { Dict } from './core'

/** Русские строки (заполняются в Задаче 2; набор ключей обязан совпадать с en). */
export const ru: Dict = {}
```

`src/shared/i18n/plurals.ts`:
```ts
import type { PluralTable } from './core'

/** Plural-form templates (filled in Task 2). en has 2 forms, ru has 3. */
export const plurals: PluralTable = {}
```

`src/shared/i18n/index.ts`:
```ts
// Public i18n API. Everything user-visible must come from here — no hardcoded UI strings.
import { en } from './en'
import { ru } from './ru'
import { plurals } from './plurals'
import { createI18n as createCoreI18n, pluralIndex, resolveLocale } from './core'

export { pluralIndex, resolveLocale }
export type {
  Catalogs,
  Dict,
  I18n,
  LangPref,
  Locale,
  Params,
  PluralForms,
  PluralTable
} from './core'
export { en } from './en'
export { ru } from './ru'
export { plurals } from './plurals'

/** Translator bound to the real en/ru catalogs. */
export function createI18n(locale: Locale): ReturnType<typeof createCoreI18n> {
  return createCoreI18n(locale, { en, ru }, plurals)
}
```

- [ ] **Step 5: Run tests to verify they pass**

Run: `npx vitest run src/shared/i18n/i18n.test.ts`
Expected: PASS (все describe-блоки зелёные).

- [ ] **Step 6: Type-check**

Run: `npx tsc --noEmit`
Expected: 0 ошибок.

- [ ] **Step 7: Commit**

```bash
git add src/shared/i18n/
git commit -m "feat(i18n): shared i18n core (resolveLocale, t, tPlural) with tests"
```

---

### Task 2: Каталоги en/ru + плюраформы

**Files:**
- Modify: `src/shared/i18n/en.ts` (заполнить полностью)
- Modify: `src/shared/i18n/ru.ts` (заполнить полностью)
- Modify: `src/shared/i18n/plurals.ts` (заполнить)
- Test: `src/shared/i18n/catalogs.test.ts` (создать)

**Interfaces:**
- Consumes: `type Dict` из `./core` (Задача 1).
- Produces: полный набор ключей, используемых Задачами 4–6. Ключ — точка-разделённый snake-in-camelCase идентификатор; en-значения совпадают с текущими английскими строками кода 1-в-1.

- [ ] **Step 1: Write the failing test — `src/shared/i18n/catalogs.test.ts`**

```ts
import { describe, expect, it } from 'vitest'
import { en } from './en'
import { plurals } from './plurals'
import { ru } from './ru'

const PLACEHOLDER = /\{(\w+)\}/g

const tokens = (s: string): string[] =>
  [...s.matchAll(PLACEHOLDER)].map((m) => m[1]).sort()

describe('catalogs', () => {
  it('ru has exactly the same keys as en', () => {
    expect(Object.keys(ru).sort()).toEqual(Object.keys(en).sort())
  })

  it('ru keeps every placeholder from en', () => {
    for (const [key, value] of Object.entries(en)) {
      expect(tokens(ru[key] ?? ''), key).toEqual(tokens(value))
    }
  })

  it('plurals: 2 en forms and 3 ru forms', () => {
    expect(Object.keys(plurals).length).toBeGreaterThan(0)
    for (const forms of Object.values(plurals)) {
      expect(forms.en).toHaveLength(2)
      expect(forms.ru).toHaveLength(3)
    }
  })
})
```

- [ ] **Step 2: Run test to verify it fails**

Run: `npx vitest run src/shared/i18n/catalogs.test.ts`
Expected: FAIL — ключи en заполнены ниже, ru пуст → наборы ключей не совпадают.

- [ ] **Step 3: Fill `src/shared/i18n/en.ts` (полная замена содержимого)**

```ts
import type { Dict } from './core'

/**
 * English source strings. en is the source of truth: every key here must also
 * exist in ru.ts (enforced by catalogs.test.ts). HTML-bearing values are used
 * ONLY via data-i18n-html on trusted, static catalog markup (<b>, <a>).
 */
export const en: Dict = {
  // --- tray ---
  'tray.tooltip': 'Quakpit',
  'tray.open': 'Open Quakpit',
  'tray.testFlight': 'Send a test flight  (⌘⇧D)',
  'tray.quit': 'Quit Quakpit',

  // --- settings chrome ---
  'settings.windowTitle': 'Quakpit — Settings',
  'settings.tab.general': 'General',
  'settings.tab.appearance': 'Appearance',
  'settings.tab.calendar': 'Calendar',
  'settings.testFlight': 'Send a test flight',
  'settings.presentedBy': 'Presented by',

  // --- general tab ---
  'settings.general.subtitle': 'When and how the duck flies by.',
  'settings.reminders': 'Reminders',
  'settings.lead.label': 'Lead time',
  'settings.lead.hint': 'Fly this long before a meeting',
  'settings.lead.5': '5 min',
  'settings.lead.15': '15 min',
  'settings.lead.30': '30 min',
  'settings.flyAtStart.label': 'Fly again at meeting time',
  'settings.flyAtStart.hint': 'A second fly-by right when it starts',
  'settings.banner.label': 'Banner message',
  'settings.banner.hint': 'Tap a tag to insert it',
  'settings.flight': 'Flight',
  'settings.speed.label': 'Speed',
  'settings.speed.hint': 'How fast the plane crosses the screen',
  'settings.display.label': 'Show on',
  'settings.display.hint': 'Which screen the plane uses',
  'settings.display.cursor': 'Screen with my cursor',
  'settings.display.primary': 'My main screen',
  'settings.launch.label': 'Launch at login',
  'settings.launch.hint': 'Start Quakpit when you sign in',
  'settings.hideDock.label': 'Hide Dock icon',
  'settings.hideDock.hint': 'Menu-bar-only app — reopen Quakpit from the tray icon',
  'settings.stay.label': 'Stay signed in',
  'settings.stay.hint': 'Store the calendar token (encrypted)',
  'settings.language.label': 'Language',
  'settings.language.hint': 'Applies immediately, no restart needed',
  'settings.language.auto': 'Auto — system language',

  // --- appearance tab ---
  'settings.appearance.subtitle': 'Make Quakpit yours.',
  'settings.preview.banner': 'Meeting in 5 minutes',
  'settings.sub.flier': 'Flier',
  'settings.sub.banner': 'Banner',
  'settings.sub.typo': 'Typo',
  'settings.sub.sound': 'Sound',
  'settings.flier.hint': 'Pick a head, then a plane colour.',
  'settings.flier.head': 'Head',
  'settings.flier.color': 'Plane colour',
  'settings.bannerTheme': 'Banner theme',
  'settings.bannerTheme.hint': 'Stripe colours of the towed banner.',
  'settings.typography': 'Typography',
  'settings.typography.hint': 'Banner font.',
  'settings.engine.label': 'Engine sound',
  'settings.engine.hint': 'Engine + sound while it flies',
  'settings.flightSound.label': 'Flight sound',
  'settings.flightSound.hint': 'Follows your animal — tap to preview',
  'option.fontPreview': 'Hello',

  // --- calendar tab ---
  'settings.calendar.subtitle': 'Read on this device only — never sent anywhere.',
  'settings.provider.byLink': 'Add by link',
  'settings.badge.easiest': 'easiest',
  'settings.provider.googleSoon': 'Sign-in coming soon',
  'settings.badge.soon': 'Soon',
  'settings.state.connected': 'Connected',
  'settings.state.connectedDetail': 'Connected · {detail}',
  'settings.state.connectedOk': '✅ Connected',
  'settings.state.notConnected': 'Not connected',
  'settings.nextUp': 'Next up',
  'settings.nextUp.refreshTitle': 'Re-check your calendars now',
  'settings.upcoming.empty': 'No meetings in the next couple of hours.',
  'settings.upcoming.connect': 'Connect a calendar to see your meetings.',
  'settings.privacy': '🔒 Everything stays on this device. Quakpit has no server.',

  // --- wizards (shared actions) ---
  'settings.back': '‹ Back',
  'settings.action.connect': 'Connect',
  'settings.action.connecting': 'Connecting…',
  'settings.action.disconnect': 'Disconnect',
  'settings.action.remove': 'Remove',

  // --- iCal-link wizard ---
  'settings.ical.title': 'Add a calendar by link',
  'settings.ical.cardTitle': 'Paste a calendar link (iCal / .ics)',
  'settings.ical.step1':
    'Google Calendar (web): your calendar → <b>Settings and sharing</b> → <b>Integrate calendar</b> → copy <b>Secret address in iCal format</b>.',
  'settings.ical.step2': 'Outlook / Fastmail / others: look for <b>Publish</b> or an <b>iCal/ICS</b> link.',
  'settings.ical.step3': 'Paste it below — add as many calendars as you like.',
  'settings.ical.linkLabel': 'Calendar link',
  'settings.ical.urlPlaceholder': 'https://…/basic.ics  (or webcal://…)',
  'settings.ical.nameLabel': 'Name (optional)',
  'settings.ical.namePlaceholder': 'Work',
  'settings.ical.add': 'Add calendar',
  'settings.ical.adding': 'Adding…',
  'settings.ical.feedsTitle': 'Your calendar links',
  'settings.ical.none': 'None yet.',

  // --- iCloud wizard ---
  'settings.icloud.title': 'Connect Apple iCloud',
  'settings.icloud.cardTitle': 'Connect with an app-specific password',
  'settings.icloud.step1':
    'Open <a href="https://appleid.apple.com" target="_blank" rel="noopener">appleid.apple.com</a> → Sign-In &amp; Security → <b>App-Specific Passwords</b> → generate one.',
  'settings.icloud.step2': 'Enter your Apple ID and that password below.',
  'settings.icloud.appleId': 'Apple ID',
  'settings.icloud.appPassword': 'App-specific password',

  // --- Exchange wizard ---
  'settings.exchange.title': 'Connect Microsoft Exchange',
  'settings.exchange.cardTitle': 'Connect to your company Exchange server',
  'settings.exchange.step1': 'Ask IT for the EWS address — usually <b>https://mail.company.com/EWS/Exchange.asmx</b>.',
  'settings.exchange.step2': 'Sign in with your Windows login, e.g. <b>CORP\\jane</b> or <b>jane@corp.com</b>.',
  'settings.exchange.step3': 'The server must be reachable — from the office network or VPN.',
  'settings.exchange.server': 'Server address',
  'settings.exchange.serverPlaceholder': 'mail.company.com',
  'settings.exchange.username': 'Username',
  'settings.exchange.userPlaceholder': 'CORP\\jane',
  'settings.exchange.password': 'Password',

  // --- option display names (looked up as `option.<group>.<id>`) ---
  'option.head.duck': 'Duck',
  'option.color.red': 'Red',
  'option.theme.classic': 'Classic',
  'option.font.system': 'System',
  'option.font.rounded': 'Rounded',
  'option.font.serif': 'Serif',
  'option.font.mono': 'Mono',
  'option.font.condensed': 'Condensed',
  'option.sound.quack': 'Duck',

  // --- main → UI strings ---
  'demo.message': 'Call with Jack in 5 minutes',
  'scheduler.startingNow': '{title} starting now',
  'banner.default': '{title} in {minutes} minutes',
  'event.untitled': 'Untitled event',
  'calendar.google.name': 'Google Calendar',
  'calendar.ical.name': 'Calendar links',
  'calendar.icloud.name': 'iCloud',
  'calendar.exchange.name': 'Exchange',
  'calendar.unknownProvider': 'Unknown provider: {id}',
  'calendar.cannotConfigure': 'Cannot configure provider: {id}',

  // --- provider errors: google ---
  'google.enterCreds': 'Enter both the Client ID and the Client secret.',
  'google.notConfigured': 'Google OAuth credentials are not configured (see README).',
  'google.tokenExchangeFailed': 'Token exchange failed ({status})',
  'google.eventsFetchFailed': 'Events fetch failed ({status})',
  'google.loopbackFailed': 'Could not open a local callback port',
  'google.signInTimeout': 'Sign-in timed out',
  'google.oauthPage.title': '🦆 Quakpit is connected!',
  'google.oauthPage.subtitle': 'You can close this tab and return to the app.',
  'google.noAuthCode': 'No authorization code returned',
  'google.cannotRefresh': 'Cannot refresh: not connected',
  'google.tokenRefreshFailed': 'Token refresh failed ({status})',
  'google.noAccessToken': 'No access token available',

  // --- provider errors: icloud ---
  'icloud.notConnected': 'iCloud not connected',
  'icloud.wrongCreds': 'Wrong Apple ID or app-specific password.',
  'icloud.davError': 'iCloud CalDAV error ({status})',
  'icloud.noAccount': 'Could not find your iCloud account.',
  'icloud.noCalendars': 'Could not find your iCloud calendars.',
  'icloud.enterCreds': 'Enter your Apple ID and an app-specific password.',
  'icloud.fallbackCalendar': 'Calendar',

  // --- provider errors: ical ---
  'ical.invalidLink': 'Enter a valid calendar link (https or webcal).',
  'ical.fetchFailed': 'Could not fetch the calendar ({status}).',
  'ical.notIcal': 'That link is not an iCal (.ics) calendar.',
  'ical.fallbackName': 'Calendar',

  // --- provider errors: exchange / ews ---
  'exchange.timeout': 'The Exchange server did not respond in time.',
  'exchange.unreachable': 'Cannot reach the Exchange server (network, VPN or untrusted certificate).',
  'exchange.status401': 'The server rejected the login. Check your username and password.',
  'exchange.status403': 'Access denied — EWS may be disabled for your account by your administrator.',
  'exchange.status404': 'EWS not found at this address. Check the server address with IT.',
  'exchange.generic': 'Exchange error ({status}).',
  'exchange.noAuthMethod': 'The server offered no supported login method (Basic or NTLM).',
  'exchange.wrongCreds': 'Wrong username, password or domain.',
  'exchange.enterAll': 'Enter the server address, your username and your password.',
  'exchange.connectFailed':
    'Could not connect: check the server address, your username and password. If EWS is disabled by your administrator, Quakpit cannot read this calendar.',
  'ews.enterAddress': 'Enter your Exchange server address.',
  'ews.badAddress': 'That does not look like a server address.',
  'ews.useHttps': 'Use an https:// address — your password is sent to this server.',

  // --- updater ---
  'updater.title': 'Update available',
  'updater.ready': 'Quakpit {version} is ready 🦆',
  'updater.restartNow': 'Restart now',
  'updater.later': 'Later',
  'updater.detail.required': 'A required update has been downloaded. Quakpit will restart to update.',
  'updater.detail.optional':
    'A new version has been downloaded. Restart Quakpit to update — or it will update next time you quit.',

  // --- dialogs ---
  'dialog.chooseImage': 'Choose a plane image',
  'dialog.images': 'Images'
}
```

- [ ] **Step 4: Fill `src/shared/i18n/ru.ts` (полная замена содержимого)**

```ts
import type { Dict } from './core'

/** Русские строки. Набор ключей обязан совпадать с en.ts — проверяет catalogs.test.ts. */
export const ru: Dict = {
  // --- tray ---
  'tray.tooltip': 'Quakpit',
  'tray.open': 'Открыть Quakpit',
  'tray.testFlight': 'Отправить тестовый полёт  (⌘⇧D)',
  'tray.quit': 'Выйти из Quakpit',

  // --- настройки: оболочка ---
  'settings.windowTitle': 'Quakpit — Настройки',
  'settings.tab.general': 'Основные',
  'settings.tab.appearance': 'Оформление',
  'settings.tab.calendar': 'Календарь',
  'settings.testFlight': 'Отправить тестовый полёт',
  'settings.presentedBy': 'От студии',

  // --- вкладка «Основные» ---
  'settings.general.subtitle': 'Когда и как прилетает утка.',
  'settings.reminders': 'Напоминания',
  'settings.lead.label': 'Время до встречи',
  'settings.lead.hint': 'За сколько до встречи вылетать',
  'settings.lead.5': '5 мин',
  'settings.lead.15': '15 мин',
  'settings.lead.30': '30 мин',
  'settings.flyAtStart.label': 'Повторить в начале встречи',
  'settings.flyAtStart.hint': 'Второй пролёт ровно в момент начала',
  'settings.banner.label': 'Текст баннера',
  'settings.banner.hint': 'Нажмите тег, чтобы вставить его',
  'settings.flight': 'Полёт',
  'settings.speed.label': 'Скорость',
  'settings.speed.hint': 'Как быстро самолёт пересекает экран',
  'settings.display.label': 'Показывать на',
  'settings.display.hint': 'Какой экран использует самолёт',
  'settings.display.cursor': 'Экран с курсором',
  'settings.display.primary': 'Основной экран',
  'settings.launch.label': 'Запускать при входе в систему',
  'settings.launch.hint': 'Запускать Quakpit вместе с macOS',
  'settings.hideDock.label': 'Скрывать значок из Dock',
  'settings.hideDock.hint': 'Приложение только в строке меню — открывайте Quakpit через значок в трее',
  'settings.stay.label': 'Оставаться в системе',
  'settings.stay.hint': 'Хранить токен календаря (зашифрованно)',
  'settings.language.label': 'Язык',
  'settings.language.hint': 'Применяется сразу, без перезапуска',
  'settings.language.auto': 'Авто — как в системе',

  // --- вкладка «Оформление» ---
  'settings.appearance.subtitle': 'Настройте Quakpit под себя.',
  'settings.preview.banner': 'Встреча через 5 минут',
  'settings.sub.flier': 'Пилот',
  'settings.sub.banner': 'Баннер',
  'settings.sub.typo': 'Шрифт',
  'settings.sub.sound': 'Звук',
  'settings.flier.hint': 'Выберите голову, затем цвет самолёта.',
  'settings.flier.head': 'Голова',
  'settings.flier.color': 'Цвет самолёта',
  'settings.bannerTheme': 'Тема баннера',
  'settings.bannerTheme.hint': 'Цветные полосы буксируемого баннера.',
  'settings.typography': 'Типографика',
  'settings.typography.hint': 'Шрифт баннера.',
  'settings.engine.label': 'Звук двигателя',
  'settings.engine.hint': 'Мотор и звуки во время полёта',
  'settings.flightSound.label': 'Звук полёта',
  'settings.flightSound.hint': 'Зависит от выбранного животного — нажмите, чтобы прослушать',
  'option.fontPreview': 'Привет',

  // --- вкладка «Календарь» ---
  'settings.calendar.subtitle': 'Читается только на этом устройстве — никуда не отправляется.',
  'settings.provider.byLink': 'Добавить по ссылке',
  'settings.badge.easiest': 'проще всего',
  'settings.provider.googleSoon': 'Вход скоро появится',
  'settings.badge.soon': 'Скоро',
  'settings.state.connected': 'Подключено',
  'settings.state.connectedDetail': 'Подключено · {detail}',
  'settings.state.connectedOk': '✅ Подключено',
  'settings.state.notConnected': 'Не подключено',
  'settings.nextUp': 'Ближайшие',
  'settings.nextUp.refreshTitle': 'Проверить календари прямо сейчас',
  'settings.upcoming.empty': 'В ближайшие пару часов встреч нет.',
  'settings.upcoming.connect': 'Подключите календарь, чтобы увидеть встречи.',
  'settings.privacy': '🔒 Всё остаётся на этом устройстве. У Quakpit нет сервера.',

  // --- визарды (общие действия) ---
  'settings.back': '‹ Назад',
  'settings.action.connect': 'Подключить',
  'settings.action.connecting': 'Подключаем…',
  'settings.action.disconnect': 'Отключить',
  'settings.action.remove': 'Удалить',

  // --- визард iCal-ссылок ---
  'settings.ical.title': 'Добавить календарь по ссылке',
  'settings.ical.cardTitle': 'Вставьте ссылку на календарь (iCal / .ics)',
  'settings.ical.step1':
    'Google Календарь (веб): откройте календарь → <b>Настройки и доступ</b> → <b>Интеграция календаря</b> → скопируйте <b>Секретный адрес в формате iCal</b>.',
  'settings.ical.step2': 'Outlook / Fastmail / другие: ищите <b>Публикация</b> или ссылку <b>iCal/ICS</b>.',
  'settings.ical.step3': 'Вставьте ссылку ниже — добавляйте столько календарей, сколько нужно.',
  'settings.ical.linkLabel': 'Ссылка на календарь',
  'settings.ical.urlPlaceholder': 'https://…/basic.ics  (или webcal://…)',
  'settings.ical.nameLabel': 'Название (необязательно)',
  'settings.ical.namePlaceholder': 'Работа',
  'settings.ical.add': 'Добавить календарь',
  'settings.ical.adding': 'Добавляем…',
  'settings.ical.feedsTitle': 'Ваши ссылки на календари',
  'settings.ical.none': 'Пока ничего нет.',

  // --- визард iCloud ---
  'settings.icloud.title': 'Подключить Apple iCloud',
  'settings.icloud.cardTitle': 'Вход с паролем приложения',
  'settings.icloud.step1':
    'Откройте <a href="https://appleid.apple.com" target="_blank" rel="noopener">appleid.apple.com</a> → «Вход и безопасность» → <b>Пароли приложений</b> → создайте пароль.',
  'settings.icloud.step2': 'Введите ниже свой Apple ID и этот пароль.',
  'settings.icloud.appleId': 'Apple ID',
  'settings.icloud.appPassword': 'Пароль приложения',

  // --- визард Exchange ---
  'settings.exchange.title': 'Подключить Microsoft Exchange',
  'settings.exchange.cardTitle': 'Подключение к корпоративному серверу Exchange',
  'settings.exchange.step1': 'Узнайте у IT адрес EWS — обычно <b>https://mail.company.com/EWS/Exchange.asmx</b>.',
  'settings.exchange.step2': 'Войдите с учётной записью Windows, например <b>CORP\\jane</b> или <b>jane@corp.com</b>.',
  'settings.exchange.step3': 'Сервер должен быть доступен — из офисной сети или через VPN.',
  'settings.exchange.server': 'Адрес сервера',
  'settings.exchange.serverPlaceholder': 'mail.company.com',
  'settings.exchange.username': 'Имя пользователя',
  'settings.exchange.userPlaceholder': 'CORP\\jane',
  'settings.exchange.password': 'Пароль',

  // --- названия опций (поиск по `option.<группа>.<id>`) ---
  'option.head.duck': 'Утка',
  'option.color.red': 'Красный',
  'option.theme.classic': 'Классика',
  'option.font.system': 'Системный',
  'option.font.rounded': 'Скруглённый',
  'option.font.serif': 'С засечками',
  'option.font.mono': 'Моноширинный',
  'option.font.condensed': 'Узкий',
  'option.sound.quack': 'Утка',

  // --- main → UI ---
  'demo.message': 'Звонок с Джеком через 5 минут',
  'scheduler.startingNow': '{title} начинается сейчас',
  'banner.default': '{title} через {minutes} мин',
  'event.untitled': 'Без названия',
  'calendar.google.name': 'Google Календарь',
  'calendar.ical.name': 'Ссылки на календари',
  'calendar.icloud.name': 'iCloud',
  'calendar.exchange.name': 'Exchange',
  'calendar.unknownProvider': 'Неизвестный провайдер: {id}',
  'calendar.cannotConfigure': 'Провайдер не поддерживает настройку: {id}',

  // --- ошибки провайдеров: google ---
  'google.enterCreds': 'Введите и Client ID, и Client secret.',
  'google.notConfigured': 'Учётные данные Google OAuth не настроены (см. README).',
  'google.tokenExchangeFailed': 'Не удалось обменять код на токен ({status})',
  'google.eventsFetchFailed': 'Не удалось получить события ({status})',
  'google.loopbackFailed': 'Не удалось открыть локальный порт для обратного вызова',
  'google.signInTimeout': 'Время ожидания входа истекло',
  'google.oauthPage.title': '🦆 Quakpit подключён!',
  'google.oauthPage.subtitle': 'Можно закрыть эту вкладку и вернуться в приложение.',
  'google.noAuthCode': 'Google не вернул код авторизации',
  'google.cannotRefresh': 'Не удалось обновить токен: нет подключения',
  'google.tokenRefreshFailed': 'Не удалось обновить токен ({status})',
  'google.noAccessToken': 'Токен доступа недоступен',

  // --- ошибки провайдеров: icloud ---
  'icloud.notConnected': 'iCloud не подключён',
  'icloud.wrongCreds': 'Неверный Apple ID или пароль приложения.',
  'icloud.davError': 'Ошибка CalDAV iCloud ({status})',
  'icloud.noAccount': 'Не удалось найти вашу учётную запись iCloud.',
  'icloud.noCalendars': 'Не удалось найти ваши календари iCloud.',
  'icloud.enterCreds': 'Введите Apple ID и пароль приложения.',
  'icloud.fallbackCalendar': 'Календарь',

  // --- ошибки провайдеров: ical ---
  'ical.invalidLink': 'Введите корректную ссылку на календарь (https или webcal).',
  'ical.fetchFailed': 'Не удалось загрузить календарь ({status}).',
  'ical.notIcal': 'По этой ссылке не iCal-календарь (.ics).',
  'ical.fallbackName': 'Календарь',

  // --- ошибки провайдеров: exchange / ews ---
  'exchange.timeout': 'Сервер Exchange не ответил за отведённое время.',
  'exchange.unreachable': 'Не удалось связаться с сервером Exchange (сеть, VPN или недоверенный сертификат).',
  'exchange.status401': 'Сервер отклонил вход. Проверьте имя пользователя и пароль.',
  'exchange.status403': 'Доступ запрещён — возможно, администратор отключил EWS для вашей учётной записи.',
  'exchange.status404': 'По этому адресу EWS не найден. Уточните адрес сервера у IT.',
  'exchange.generic': 'Ошибка Exchange ({status}).',
  'exchange.noAuthMethod': 'Сервер не предложил поддерживаемый способ входа (Basic или NTLM).',
  'exchange.wrongCreds': 'Неверное имя пользователя, пароль или домен.',
  'exchange.enterAll': 'Введите адрес сервера, имя пользователя и пароль.',
  'exchange.connectFailed':
    'Не удалось подключиться: проверьте адрес сервера, имя пользователя и пароль. Если администратор отключил EWS, Quakpit не сможет читать этот календарь.',
  'ews.enterAddress': 'Введите адрес сервера Exchange.',
  'ews.badAddress': 'Это не похоже на адрес сервера.',
  'ews.useHttps': 'Используйте адрес https:// — пароль отправляется на этот сервер.',

  // --- обновление ---
  'updater.title': 'Доступно обновление',
  'updater.ready': 'Quakpit {version} готов 🦆',
  'updater.restartNow': 'Перезапустить сейчас',
  'updater.later': 'Позже',
  'updater.detail.required': 'Загружено обязательное обновление. Quakpit перезапустится, чтобы установить его.',
  'updater.detail.optional':
    'Загружена новая версия. Перезапустите Quakpit для обновления — или оно установится при следующем закрытии приложения.',

  // --- диалоги ---
  'dialog.chooseImage': 'Выберите изображение самолёта',
  'dialog.images': 'Изображения'
}
```

- [ ] **Step 5: Fill `src/shared/i18n/plurals.ts` (полная замена содержимого)**

```ts
import type { PluralTable } from './core'

/**
 * Plural-form templates, keyed like dict entries. en has [one, other] forms,
 * ru has [one, few, many] — index selection lives in core.pluralIndex().
 */
export const plurals: Record<string, { en: [string, string]; ru: [string, string, string] }> = {
  'ical.feeds': {
    en: ['{n} calendar', '{n} calendars'],
    ru: ['{n} календарь', '{n} календаря', '{n} календарей']
  }
}
```

- [ ] **Step 6: Run tests to verify they pass**

Run: `npx vitest run src/shared/i18n/`
Expected: PASS — и `i18n.test.ts`, и `catalogs.test.ts` зелёные.

- [ ] **Step 7: Type-check + full test suite**

Run: `npx tsc --noEmit && npm test`
Expected: 0 ошибок типизации, все тесты зелёные (включая существующие `ews.test.ts`, `generate-tray-icon.test.ts`).

- [ ] **Step 8: Commit**

```bash
git add src/shared/i18n/
git commit -m "feat(i18n): full en/ru string catalogs + plural forms"
```

---

### Task 3: Настройка языка в prefs + локализованный дефолт баннера

**Files:**
- Modify: `src/main/store.ts` (тип `Prefs`, `DEFAULT_PREFS` → `defaultPrefs(lang)`, `getPrefs`)
- Modify: `src/renderer/quakpit.d.ts:16-33` (добавить `lang` в тип `Prefs`)

**Interfaces:**
- Consumes: `resolveLocale`, `createI18n`, `type LangPref` из `../shared/i18n` (Задача 1), ключ `banner.default` (Задача 2).
- Produces: `Prefs.lang: LangPref` — читается Задачами 4–6; `getPrefs().lang` в main и renderer.

- [ ] **Step 1: Modify `src/main/store.ts` — тип, дефолты, чтение**

Вверху файла добавить импорт (после существующих импортов `electron`/`node`):

```ts
import { createI18n, resolveLocale, type LangPref } from '../shared/i18n'
```

В тип `Prefs` добавить поле (после `customFlierName: string`):

```ts
  customFlierName: string // legacy (kept so old prefs files still parse)
  lang: LangPref // UI language: 'auto' = follow the system, else explicit
```

Заменить `DEFAULT_PREFS` (строки 26–43) и `getPrefs` (строки 61–74) на:

```ts
// Locale-independent defaults; only messageTemplate depends on the language.
const BASE_DEFAULTS = {
  leadMinutes: 5,
  soundEnabled: true,
  staySignedIn: true,
  launchAtLogin: false,
  hideFromDock: false,
  targetDisplay: 'cursor' as const,
  theme: 'classic',
  flier: 'duck-plane',
  font: 'system',
  speed: 'normal' as const,
  flyAtStart: false,
  soundPack: 'quack',
  flierHead: 'duck',
  flierColor: 'red',
  customFlierName: ''
}

function defaultPrefs(lang: LangPref): Prefs {
  const locale = resolveLocale(lang, app.getLocale())
  return {
    ...BASE_DEFAULTS,
    messageTemplate: createI18n(locale).t('banner.default'),
    lang
  }
}

function sanitizeLang(value: unknown): LangPref {
  return value === 'ru' || value === 'en' ? value : 'auto'
}

export function getPrefs(): Prefs {
  if (cache) return cache
  let raw: Partial<Prefs> = {}
  try {
    if (existsSync(prefsPath())) {
      raw = JSON.parse(readFileSync(prefsPath(), 'utf8')) as Partial<Prefs>
    }
  } catch {
    raw = {}
  }
  // Defaults (with a localized banner template) sit underneath whatever was saved —
  // an existing user's messageTemplate is NEVER rewritten.
  cache = { ...defaultPrefs(sanitizeLang(raw.lang)), ...raw }
  return cache as Prefs
}
```

Остальное содержимое `store.ts` (setPrefs, *.bin-хелперы) не меняется. Внимание: `messageTemplate` из `DEFAULT_PREFS` больше не константа — проверь, что в файле не осталось ссылок на `DEFAULT_PREFS` (единственное использование было в `getPrefs`).

- [ ] **Step 2: Modify `src/renderer/quakpit.d.ts` — добавить поле в тип**

В блоке `type Prefs = {` (после `customFlierName: string`) добавить:

```ts
    customFlierName: string
    lang: 'auto' | 'ru' | 'en'
```

- [ ] **Step 3: Type-check**

Run: `npx tsc --noEmit`
Expected: 0 ошибок. (Если ошибка в `settings.ts` — там есть локальный fallback-объект `Prefs`, поле `lang` добавит Задача 6; если `tsc` ругается на `settings.ts:112` — добавь `lang: 'auto'` в тот литерал уже в этой задаче.)

- [ ] **Step 4: Commit**

```bash
git add src/main/store.ts src/renderer/quakpit.d.ts src/renderer/settings/settings.ts
git commit -m "feat(prefs): language preference (auto/ru/en) + localized default banner template"
```

---

### Task 4: Локализация main-процесса: обёртка t(), tray, updater, диалоги, сообщения

**Files:**
- Create: `src/main/i18n.ts`
- Modify: `src/main/tray.ts` (полная замена — плюс `rebuildTray`)
- Modify: `src/main/ipc.ts:29-45` (хук пересборки трея), `:89-97` (демо-полёт), `:100-105` (диалог выбора картинки)
- Modify: `src/main/updater.ts:1-2` (импорт), `:22-35` (строки диалога)
- Modify: `src/main/scheduler.ts:1-3` (импорт), `:74` (сообщение «starting now»)
- Modify: `src/main/index.ts:26-32` (демо-сообщение)

**Interfaces:**
- Consumes: `createI18n`, `resolveLocale` из `../shared/i18n` (Задача 1), `getPrefs().lang` (Задача 3).
- Produces: `currentLocale(): Locale`, `t(key, params?): string`, `tPlural(key, n, params?): string` из `src/main/i18n.ts` — их используют Задача 5 и обновлённые tray/ipc/updater/scheduler; `rebuildTray(): void` из `src/main/tray.ts` — использует ipc.ts.

- [ ] **Step 1: Create `src/main/i18n.ts`**

```ts
// Locale resolution for the main process: explicit preference wins, else the
// system language. t() re-reads prefs on every call, so a language switch in
// the settings window applies to everything main produces afterwards.
import { app } from 'electron'
import { getPrefs } from './store'
import { createI18n, resolveLocale, type I18n, type Locale, type Params } from '../shared/i18n'

let cache: { locale: Locale; i18n: ReturnType<typeof createI18n> } | null = null

/** Effective locale: explicit preference, else the OS language. */
export function currentLocale(): Locale {
  return resolveLocale(getPrefs().lang, app.getLocale())
}

/** getI18n() cached until the resolved locale changes. */
export function getI18n(): ReturnType<typeof createI18n> {
  const locale = currentLocale()
  if (!cache || cache.locale !== locale) cache = { locale, i18n: createI18n(locale) }
  return cache.i18n
}

export function t(key: string, params?: Params): string {
  return getI18n().t(key, params)
}

export function tPlural(key: string, n: number, params?: Params): string {
  return getI18n().tPlural(key, n, params)
}
```

- [ ] **Step 2: Rewrite `src/main/tray.ts` (сохранить логику иконки, добавить rebuildTray)**

```ts
import { Tray, Menu, nativeImage, app } from 'electron'
import { join } from 'node:path'
import { t } from './i18n'

let tray: Tray | null = null
let onTestFlight: (() => void) | null = null
let onSettings: (() => void) | null = null

function buildMenu(): Menu {
  return Menu.buildFromTemplate([
    { label: t('tray.open'), click: () => onSettings?.() },
    { label: t('tray.testFlight'), click: () => onTestFlight?.() },
    { type: 'separator' },
    { label: t('tray.quit'), click: () => app.quit() }
  ])
}

/** Creates the menu-bar / system-tray icon and its menu. */
export function createTray(testFlightCb: () => void, settingsCb: () => void): Tray {
  onTestFlight = testFlightCb
  onSettings = settingsCb
  // The icons are generated by scripts/generate-tray-icon.cjs into build/ (dev)
  // and shipped via extraResources into Resources/ (packaged app).
  // createFromPath automatically picks up the @2x variant when present.
  const iconPath = app.isPackaged
    ? join(process.resourcesPath, 'iconTemplate.png')
    : join(app.getAppPath(), 'build', 'iconTemplate.png')
  let image = nativeImage.createFromPath(iconPath)
  if (image.isEmpty()) {
    image = nativeImage.createEmpty()
  }
  // Template image = macOS auto-adapts it to light/dark menu bars.
  if (process.platform === 'darwin') {
    image.setTemplateImage(true)
  }

  tray = new Tray(image)
  tray.setToolTip(t('tray.tooltip'))
  tray.setContextMenu(buildMenu())
  return tray
}

/** Re-applies localized labels after a language change (icon is kept). */
export function rebuildTray(): void {
  if (!tray) return
  tray.setToolTip(t('tray.tooltip'))
  tray.setContextMenu(buildMenu())
}
```

- [ ] **Step 3: `src/main/ipc.ts` — импорт, хук языка, строки демо и диалога**

В импорты добавить `import { t } from './i18n'` и `import { rebuildTray } from './tray'`.

В обработчик `prefs:set` (после блока `staySignedIn`, перед `return prefs`) вставить:

```ts
    if (patch.lang !== undefined) rebuildTray()
```

В `flight:test` заменить литерал сообщения:

```ts
    flyAcross({
      message: t('demo.message'),
      durationMs: 9000,
      sound: prefs.soundEnabled
    })
```

В `flier:import` заменить заголовок и фильтр:

```ts
      title: t('dialog.chooseImage'),
      properties: ['openFile'],
      filters: [{ name: t('dialog.images'), extensions: ['png', 'jpg', 'jpeg', 'gif', 'webp'] }]
```

- [ ] **Step 4: `src/main/updater.ts` — строки диалога обновления**

Добавить импорт `import { t } from './i18n'` (после импорта electron-updater). Заменить колбэк `update-downloaded` на:

```ts
    autoUpdater.on('update-downloaded', (info) => {
      const restartLabel = t('updater.restartNow')
      const laterLabel = t('updater.later')
      const buttons = FORCE_UPDATE ? [restartLabel] : [restartLabel, laterLabel]
      void dialog
        .showMessageBox({
          type: 'info',
          buttons,
          defaultId: 0,
          cancelId: FORCE_UPDATE ? 0 : 1,
          title: t('updater.title'),
          message: t('updater.ready', { version: info.version }),
          detail: FORCE_UPDATE ? t('updater.detail.required') : t('updater.detail.optional')
        })
        .then((r) => {
          if (FORCE_UPDATE || r.response === 0) autoUpdater.quitAndInstall()
        })
    })
```

Остальной файл не меняется.

- [ ] **Step 5: `src/main/scheduler.ts` — сообщение «starting now»**

Добавить импорт `import { t } from './i18n'` (после строки 3). Заменить в `tick()` (строка 74):

```ts
        flyAcross({
          message: t('scheduler.startingNow', { title: ev.title }),
          durationMs: 9000,
          sound: prefs.soundEnabled
        })
```

Логику `fired`/`leadKey`/`startKey` не трогать (id-ключи с `:`).

- [ ] **Step 6: `src/main/index.ts` — демо-полёт**

Добавить импорт `import { t } from './i18n'` (после импорта store). В `sendTestFlight()` (строка 28) заменить:

```ts
    message: t('demo.message'),
```

- [ ] **Step 7: `src/main/ipc.ts` — проверка, что `prefs:set` не ломает порядок**

Хук языка добавлен в Step 3; убедиться, что блок в `prefs:set` выглядит так (новая ветка последней, порядок остальных веток не важен):

```ts
  ipcMain.handle('prefs:set', (_e, patch: Partial<Prefs>) => {
    const prefs = setPrefs(patch)
    if (patch.launchAtLogin !== undefined && app.isPackaged) {
      try {
        app.setLoginItemSettings({ openAtLogin: patch.launchAtLogin })
      } catch {
        /* ignore: not permitted in dev / sandboxed runs */
      }
    }
    if (patch.hideFromDock !== undefined && process.platform === 'darwin') {
      if (patch.hideFromDock) void app.dock?.hide()
      else void app.dock?.show()
    }
    if (patch.staySignedIn === true) google.persistIfPossible()
    if (patch.staySignedIn === false) google.forgetPersisted()
    if (patch.lang !== undefined) rebuildTray()
    return prefs
  })
```

- [ ] **Step 8: Type-check + tests**

Run: `npx tsc --noEmit && npm test`
Expected: 0 ошибок, все тесты зелёные.

- [ ] **Step 9: Commit**

```bash
git add src/main/
git commit -m "feat(main): localized tray, updater dialog, demo/test-flight messages, image dialog"
```

---

### Task 5: Локализация провайдеров календарей (имена, ошибки, плюрализация)

**Files:**
- Modify: `src/main/calendar/index.ts:24,69,86` (имя провайдера, unknown/cannot-configure)
- Modify: `src/main/calendar/google.ts` (12 строк-сообщений; **НЕ трогать строки 105–112 и 243–248**)
- Modify: `src/main/calendar/icloud.ts` (10 строк)
- Modify: `src/main/calendar/ical.ts` (7 строк, включая плюрализацию)
- Modify: `src/main/calendar/exchange.ts` (12 строк + передача локали в ews-хелперы)
- Modify: `src/main/calendar/ews.ts` (параметр `locale`, 3 строки ошибок + «Untitled event»)
- Test: `src/main/calendar/ews.test.ts` — существующий, должен пройти БЕЗ изменений (дефолтная локаль `'en'`)

**Interfaces:**
- Consumes: `t`, `tPlural`, `currentLocale` из `../i18n` (Задача 4); `createI18n`, `type Locale` из `../../shared/i18n` (Задача 1).
- Produces: `normalizeServerUrl(raw, locale?)` и `parseFindItemResponse(xml, locale?)` в `ews.ts` — новые необязательные вторые параметры (дефолт `'en'`, существующие тесты не меняются).

- [ ] **Step 1: `src/main/calendar/index.ts` — имя провайдера и ошибки фасада**

Добавить импорт `import { t } from '../i18n'` (после существующих импортов). Заменить:

- строка 24: `name: 'Google Calendar',` → `name: t('calendar.google.name'),`
- строка 69: `` throw new Error(`Unknown provider: ${provider}`) `` → `throw new Error(t('calendar.unknownProvider', { id: provider }))`
- строка 86: `` throw new Error(`Cannot configure provider: ${provider}`) `` → `throw new Error(t('calendar.cannotConfigure', { id: provider }))`

- [ ] **Step 2: `src/main/calendar/google.ts` — только перечисленные замены**

Добавить импорт `import { t } from '../i18n'` (после импорта store). Затем точечно:

| Строка | Было | Стало |
|---|---|---|
| 68 | `throw new Error('Enter both the Client ID and the Client secret.')` | `throw new Error(t('google.enterCreds'))` |
| 99 | `throw new Error('Google OAuth credentials are not configured (see README).')` | `throw new Error(t('google.notConfigured'))` |
| 118 | `` throw new Error(`Token exchange failed (${res.status})`) `` | `throw new Error(t('google.tokenExchangeFailed', { status: res.status }))` |
| 165 | `throw new Error(`` `Events fetch failed (${res.status})` ``)`` | `throw new Error(t('google.eventsFetchFailed', { status: res.status }))` |
| 183 | `title: it.summary ?? 'Untitled event',` | `title: it.summary ?? t('event.untitled'),` |
| 202 | `reject(new Error('Could not open a local callback port'))` | `reject(new Error(t('google.loopbackFailed')))` |
| 220 | `reject(new Error('Sign-in timed out'))` | `reject(new Error(t('google.signInTimeout')))` |
| 229 | HTML-страница с «🦆 Quakpit is connected!» (см. ниже) | шаблон с t() |
| 234 | `reject(new Error(error ?? 'No authorization code returned'))` | `reject(new Error(error ?? t('google.noAuthCode')))` |
| 242 | `throw new Error('Cannot refresh: not connected')` | `throw new Error(t('google.cannotRefresh'))` |
| 254 | `throw new Error(`Token refresh failed (${res.status})`)` | `throw new Error(t('google.tokenRefreshFailed', { status: res.status }))` |
| 264 | `throw new Error('No access token available')` | `throw new Error(t('google.noAccessToken'))` |

Замена строки 228–230 (HTML-страница подтверждения OAuth; это единственное место с `oauthPage.*`):

```ts
        res.writeHead(200, { 'Content-Type': 'text/html' })
        res.end(
          `<!doctype html><html lang="${currentLocale()}"><body style="font-family:-apple-system,sans-serif;text-align:center;padding-top:64px;background:#0b1b2b;color:#fff"><h2>${t('google.oauthPage.title')}</h2><p>${t('google.oauthPage.subtitle')}</p></body></html>`
        )
```

(потребуется импорт `currentLocale` рядом с `t`). **Запрет: строки 105–112 и 243–248 (объекты `body` с `client_secret`/`refresh_token`) не изменять вообще.**

- [ ] **Step 3: `src/main/calendar/icloud.ts` — точечные замены**

Добавить импорт `import { t } from '../i18n'` (после импорта store). Замены:

| Строка | Было | Стало |
|---|---|---|
| 28, 55 | `throw new Error('iCloud not connected')` | `throw new Error(t('icloud.notConnected'))` |
| 39 | `throw new Error('Wrong Apple ID or app-specific password.')` | `throw new Error(t('icloud.wrongCreds'))` |
| 40 | `` throw new Error(`iCloud CalDAV error (${res.status})`) `` | `throw new Error(t('icloud.davError', { status: res.status }))` |
| 64 | `throw new Error('Could not find your iCloud account.')` | `throw new Error(t('icloud.noAccount'))` |
| 74 | `throw new Error('Could not find your iCloud calendars.')` | `throw new Error(t('icloud.noCalendars'))` |
| 95 | `name: (name?.[1] ?? 'Calendar').trim()` | `name: (name?.[1] ?? t('icloud.fallbackCalendar')).trim()` |
| 123 | `title: ev.summary || 'Untitled event',` | `title: ev.summary || t('event.untitled'),` |
| 143 | `name: 'iCloud',` | `name: t('calendar.icloud.name'),` |
| 154 | `throw new Error('Enter your Apple ID and an app-specific password.')` | `throw new Error(t('icloud.enterCreds'))` |

- [ ] **Step 4: `src/main/calendar/ical.ts` — ошибки, плюрализация, фолбэк-имя**

Добавить импорт `import { t, tPlural } from '../i18n'` (после импорта store). Замены:

| Строка | Было | Стало |
|---|---|---|
| 28 | `throw new Error('Enter a valid calendar link (https or webcal).')` | `throw new Error(t('ical.invalidLink'))` |
| 30 | `` throw new Error(`Could not fetch the calendar (${res.status}).`) `` | `throw new Error(t('ical.fetchFailed', { status: res.status }))` |
| 32 | `throw new Error('That link is not an iCal (.ics) calendar.')` | `throw new Error(t('ical.notIcal'))` |
| 47 | `const title = ev.summary || 'Untitled event'` | `const title = ev.summary || t('event.untitled')` |
| 79 | `name: 'Calendar links',` | `name: t('calendar.ical.name'),` |
| 81 | `detail: n ? \`${n} calendar${n > 1 ? 's' : ''}\` : null,` | `detail: n ? tPlural('ical.feeds', n) : null,` |
| 95 | `nm = m ? m[1].trim().slice(0, 60) : 'Calendar'` | `nm = m ? m[1].trim().slice(0, 60) : t('ical.fallbackName')` |

- [ ] **Step 5: `src/main/calendar/ews.ts` — необязательный параметр локали (файл остаётся чистым от Electron)**

Вверху добавить:

```ts
import { createI18n, type Locale } from '../../shared/i18n'
```

Сигнатуры и использование (дефолт `'en'` сохраняет совместимость с существующим `ews.test.ts`):

```ts
export function normalizeServerUrl(raw: string, locale: Locale = 'en'): string {
  const i18n = createI18n(locale)
  const trimmed = raw.trim()
  if (!trimmed) throw new Error(i18n.t('ews.enterAddress'))
  const withScheme = /^[a-z][a-z0-9+.-]*:\/\//i.test(trimmed) ? trimmed : `https://${trimmed}`
  let url: URL
  try {
    url = new URL(withScheme)
  } catch {
    throw new Error(i18n.t('ews.badAddress'))
  }
  if (url.protocol !== 'https:') {
    throw new Error(i18n.t('ews.useHttps'))
  }
  // ...остаток функции без изменений
```

```ts
export function parseFindItemResponse(xml: string, locale: Locale = 'en'): ExchangeEvent[] {
  const i18n = createI18n(locale)
  // ...внутри цикла:
  const title = subject ? decodeEntities(subject.trim()) : i18n.t('event.untitled')
```

- [ ] **Step 6: `src/main/calendar/exchange.ts` — сообщения + передача локали**

Добавить импорт `import { currentLocale, t } from '../i18n'` (после импорта ews). Замены:

| Строка | Было | Стало |
|---|---|---|
| 57 | `throw new Error('The Exchange server did not respond in time.')` | `throw new Error(t('exchange.timeout'))` |
| 59–61 | `throw new Error('Cannot reach the Exchange server (network, VPN or untrusted certificate).')` | `throw new Error(t('exchange.unreachable'))` |
| 84 | тот же unreachable в `postViaNtlm` | `reject(new Error(t('exchange.unreachable')))` |
| 95–98 | тело `describeStatus` | см. ниже |
| 104 | `return new Error('The server offered no supported login method (Basic or NTLM).')` | `return new Error(t('exchange.noAuthMethod'))` |
| 106 | `return new Error('Wrong username, password or domain.')` | `return new Error(t('exchange.wrongCreds'))` |
| 137 | `name: 'Exchange',` | `name: t('calendar.exchange.name'),` |
| 153 | `throw new Error('Enter the server address, your username and your password.')` | `throw new Error(t('exchange.enterAll'))` |
| 149 | `normalizeServerUrl(params.serverUrl ?? '')` | `normalizeServerUrl(params.serverUrl ?? '', currentLocale())` |
| 162–165 | составное сообщение connect | см. ниже |
| 187 | `return parseFindItemResponse(body)` | `return parseFindItemResponse(body, currentLocale())` |

`describeStatus` целиком:

```ts
function describeStatus(status: number): string {
  if (status === 401) return t('exchange.status401')
  if (status === 403) return t('exchange.status403')
  if (status === 404) return t('exchange.status404')
  return t('exchange.generic', { status })
}
```

Блок `connect` с составной ошибкой:

```ts
  try {
    await postEws(candidate, probe)
  } catch {
    throw new Error(t('exchange.connectFailed'))
  }
```

- [ ] **Step 7: Run tests (ews-тесты обязательны)**

Run: `npx vitest run src/main/calendar/ews.test.ts && npx tsc --noEmit && npm test`
Expected: PASS — `ews.test.ts` зелёный (дефолтная локаль `'en'` сохраняет старые английские сообщения), типизация и весь набор тестов зелёные.

- [ ] **Step 8: Commit**

```bash
git add src/main/calendar/
git commit -m "feat(calendar): localized provider names and error messages (incl. EWS locale param)"
```

---

### Task 6: Окно настроек — data-i18n-разметка, апликатор, переключатель языка

**Files:**
- Create: `src/renderer/settings/i18n-apply.ts`
- Modify: `src/renderer/settings/index.html` (полная замена — атрибуты `data-i18n*` + новые span-обёртки)
- Modify: `src/renderer/settings/settings.ts` (точечные правки E1–E10 ниже)

**Interfaces:**
- Consumes: `createI18n`, `resolveLocale`, `type I18n` из `../../shared/i18n` (Задача 1); `Prefs.lang` (Задача 3).
- Produces: `applyI18n(root: Document, i18n: I18n): void`; живой переключатель языка. Overlay не меняется.

- [ ] **Step 1: Create `src/renderer/settings/i18n-apply.ts`**

```ts
// Applies catalog strings to the static settings markup.
//   data-i18n="key"        → textContent (plain text; never put SVG inside these elements)
//   data-i18n-html="key"   → innerHTML (trusted static catalog markup only: <b>, <a>)
//   data-i18n-placeholder  → input placeholder
//   data-i18n-title        → title + aria-label (icon-only buttons)
import type { I18n } from '../../shared/i18n'

export function applyI18n(root: Document, i18n: I18n): void {
  root.querySelectorAll<HTMLElement>('[data-i18n]').forEach((el) => {
    el.textContent = i18n.t(el.dataset.i18n as string)
  })
  root.querySelectorAll<HTMLElement>('[data-i18n-html]').forEach((el) => {
    el.innerHTML = i18n.t(el.dataset.i18nHtml as string)
  })
  root.querySelectorAll<HTMLInputElement>('[data-i18n-placeholder]').forEach((el) => {
    el.placeholder = i18n.t(el.dataset.i18nPlaceholder as string)
  })
  root.querySelectorAll<HTMLElement>('[data-i18n-title]').forEach((el) => {
    const text = i18n.t(el.dataset.i18nTitle as string)
    el.title = text
    el.setAttribute('aria-label', text)
  })
}
```

- [ ] **Step 2: Replace `src/renderer/settings/index.html` целиком**

Ключевые изменения против старого файла: у текстов появились `data-i18n*`-атрибуты; тексты рядом с SVG обёрнуты в `<span>` (чтобы `applyI18n` не стирал иконки); добавлен селект языка в карточку системных переключателей; шаги визардов идут через `data-i18n-html`. **Исправление попутное, в рамках задачи:** в старом файле было ДВЕ одинаковые кнопки «Apple iCloud» (копипаст-дубль, строки 215–229 старого файла) — в новом файле оставлена одна; задокументировать в README как исправление форка.

```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta
      http-equiv="Content-Security-Policy"
      content="default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; connect-src 'self' ws: wss:;"
    />
    <title>Quakpit — Settings</title>
    <link rel="stylesheet" href="./settings.css" />
  </head>
  <body>
    <div class="app">
      <aside class="sidebar">
        <div class="brand">
          <img id="brand-logo" class="brand-logo" alt="" />
          <span class="brand-name">Quakpit</span>
        </div>

        <nav class="nav">
          <button class="nav-item active" data-tab="general">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="21" y1="6" x2="3" y2="6"/><line x1="21" y1="12" x2="9" y2="12"/><line x1="21" y1="18" x2="15" y2="18"/><circle cx="17" cy="6" r="2"/><circle cx="5" cy="12" r="2"/><circle cx="11" cy="18" r="2"/></svg>
            <span data-i18n="settings.tab.general">General</span>
          </button>
          <button class="nav-item" data-tab="appearance">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 3v3M12 18v3M3 12h3M18 12h3M5.6 5.6l2.1 2.1M16.3 16.3l2.1 2.1M18.4 5.6l-2.1 2.1M7.7 16.3l-2.1 2.1"/><circle cx="12" cy="12" r="3"/></svg>
            <span data-i18n="settings.tab.appearance">Appearance</span>
          </button>
          <button class="nav-item" data-tab="calendar">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><rect x="3" y="4" width="18" height="18" rx="2"/><line x1="3" y1="10" x2="21" y2="10"/><line x1="8" y1="2" x2="8" y2="6"/><line x1="16" y1="2" x2="16" y2="6"/></svg>
            <span data-i18n="settings.tab.calendar">Calendar</span>
          </button>
        </nav>

        <div class="sidebar-foot">
          <button id="test-btn" class="btn btn-outline btn-block" data-i18n="settings.testFlight">Send a test flight</button>
          <a id="made-by" class="made-by" href="https://ooble.studio" rel="noopener">
            <span data-i18n="settings.presentedBy">Presented by</span> <b>Ooble&nbsp;Studio</b>
          </a>
        </div>
      </aside>

      <main class="content">
        <!-- GENERAL -->
        <section class="panel active" data-panel="general">
          <div class="panel-head"><h1 data-i18n="settings.tab.general">General</h1><p class="muted" data-i18n="settings.general.subtitle">When and how the duck flies by.</p></div>

          <!-- Reminders: timing + what the banner says -->
          <div class="card">
            <div class="card-title" data-i18n="settings.reminders">Reminders</div>
            <div class="field field-col">
              <div class="field-label"><span data-i18n="settings.lead.label">Lead time</span><span class="hint" data-i18n="settings.lead.hint">Fly this long before a meeting</span></div>
              <div id="lead" class="choices">
                <button class="choice" data-value="5" data-i18n="settings.lead.5">5 min</button>
                <button class="choice" data-value="15" data-i18n="settings.lead.15">15 min</button>
                <button class="choice" data-value="30" data-i18n="settings.lead.30">30 min</button>
              </div>
            </div>
            <div class="sep"></div>
            <div class="switch-row">
              <div class="field-label"><span data-i18n="settings.flyAtStart.label">Fly again at meeting time</span><span class="hint" data-i18n="settings.flyAtStart.hint">A second fly-by right when it starts</span></div>
              <label class="switch"><input id="flyatstart" type="checkbox" /><span class="track"><span class="thumb"></span></span></label>
            </div>
            <div class="sep"></div>
            <div class="field field-col">
              <div class="field-label"><span data-i18n="settings.banner.label">Banner message</span><span class="hint" data-i18n="settings.banner.hint">Tap a tag to insert it</span></div>
              <input id="template" class="input" type="text" />
              <div class="tag-row">
                <button class="tag-btn" data-token="{title}">title</button>
                <button class="tag-btn" data-token="{minutes}">minutes</button>
              </div>
            </div>
          </div>

          <!-- Flight: how it crosses the screen -->
          <div class="card">
            <div class="card-title" data-i18n="settings.flight">Flight</div>
            <div class="field field-col">
              <div class="field-label"><span data-i18n="settings.speed.label">Speed</span><span class="hint" data-i18n="settings.speed.hint">How fast the plane crosses the screen</span></div>
              <div id="speed" class="choices">
                <button class="choice" data-value="normal">×1</button>
                <button class="choice" data-value="fast">×1.5</button>
                <button class="choice" data-value="ultra">×2</button>
              </div>
            </div>
            <div class="sep"></div>
            <div class="field">
              <div class="field-label"><span data-i18n="settings.display.label">Show on</span><span class="hint" data-i18n="settings.display.hint">Which screen the plane uses</span></div>
              <select id="display" class="select">
                <option value="cursor" data-i18n="settings.display.cursor">Screen with my cursor</option>
                <option value="primary" data-i18n="settings.display.primary">My main screen</option>
              </select>
            </div>
          </div>

          <!-- Startup / system -->
          <div class="card">
            <div class="switch-row">
              <div class="field-label"><span data-i18n="settings.launch.label">Launch at login</span><span class="hint" data-i18n="settings.launch.hint">Start Quakpit when you sign in</span></div>
              <label class="switch"><input id="login" type="checkbox" /><span class="track"><span class="thumb"></span></span></label>
            </div>
            <div class="sep"></div>
            <div class="switch-row">
              <div class="field-label"><span data-i18n="settings.hideDock.label">Hide Dock icon</span><span class="hint" data-i18n="settings.hideDock.hint">Menu-bar-only app — reopen Quakpit from the tray icon</span></div>
              <label class="switch"><input id="hide-dock" type="checkbox" /><span class="track"><span class="thumb"></span></span></label>
            </div>
            <div class="sep"></div>
            <div class="switch-row">
              <div class="field-label"><span data-i18n="settings.stay.label">Stay signed in</span><span class="hint" data-i18n="settings.stay.hint">Store the calendar token (encrypted)</span></div>
              <label class="switch"><input id="stay" type="checkbox" /><span class="track"><span class="thumb"></span></span></label>
            </div>
            <div class="sep"></div>
            <div class="field">
              <div class="field-label"><span data-i18n="settings.language.label">Language</span><span class="hint" data-i18n="settings.language.hint">Applies immediately, no restart needed</span></div>
              <select id="lang" class="select">
                <option value="auto" data-i18n="settings.language.auto">Auto — system language</option>
                <option value="ru">Русский</option>
                <option value="en">English</option>
              </select>
            </div>
          </div>
        </section>

        <!-- APPEARANCE -->
        <section class="panel" data-panel="appearance">
          <div class="panel-head"><h1 data-i18n="settings.tab.appearance">Appearance</h1><p class="muted" data-i18n="settings.appearance.subtitle">Make Quakpit yours.</p></div>

          <!-- Live preview: the plane flies in place, clouds drift, no sound -->
          <div id="appearance-preview" class="sky">
            <div class="clouds"></div>
            <div class="preview-rig">
              <div class="banner"><span id="preview-text" data-i18n="settings.preview.banner">Meeting in 5 minutes</span></div>
              <div class="rope"></div>
              <div class="aircraft">
                <img class="plane" alt="" />
                <img class="head" alt="" />
                <img class="prop" alt="" />
              </div>
            </div>
            <svg class="filters" aria-hidden="true" width="0" height="0">
              <defs>
                <filter id="banner-wave" x="-20%" y="-60%" width="140%" height="220%">
                  <feTurbulence type="fractalNoise" baseFrequency="0.011 0.024" numOctaves="1" seed="7" result="noise" />
                  <feOffset in="noise" dx="0" dy="0" result="noiseShift">
                    <animate attributeName="dx" dur="3.2s" values="0;110;0" calcMode="spline" keyTimes="0;0.5;1" keySplines="0.45 0 0.55 1;0.45 0 0.55 1" repeatCount="indefinite" />
                    <animate attributeName="dy" dur="2.4s" values="0;26;0" calcMode="spline" keyTimes="0;0.5;1" keySplines="0.45 0 0.55 1;0.45 0 0.55 1" repeatCount="indefinite" />
                  </feOffset>
                  <feDisplacementMap in="SourceGraphic" in2="noiseShift" scale="20" xChannelSelector="R" yChannelSelector="G">
                    <animate attributeName="scale" dur="2.2s" values="16;30;16" calcMode="spline" keyTimes="0;0.5;1" keySplines="0.45 0 0.55 1;0.45 0 0.55 1" repeatCount="indefinite" />
                  </feDisplacementMap>
                </filter>
              </defs>
            </svg>
          </div>

          <!-- Mini category nav -->
          <div class="subnav" role="tablist">
            <button class="subnav-item active" data-sub="flier" data-i18n="settings.sub.flier">Flier</button>
            <button class="subnav-item" data-sub="banner" data-i18n="settings.sub.banner">Banner</button>
            <button class="subnav-item" data-sub="typo" data-i18n="settings.sub.typo">Typo</button>
            <button class="subnav-item" data-sub="sound" data-i18n="settings.sub.sound">Sound</button>
          </div>

          <!-- Flier -->
          <div class="subpanel active" data-sub="flier">
            <div class="card">
              <div class="card-title" data-i18n="settings.sub.flier">Flier</div>
              <p class="muted small" data-i18n="settings.flier.hint">Pick a head, then a plane colour.</p>
              <div class="group-label"><span data-i18n="settings.flier.head">Head</span></div>
              <div id="heads" class="swatch-grid"></div>
              <div class="group-label" style="margin-top:16px"><span data-i18n="settings.flier.color">Plane colour</span></div>
              <div id="colors" class="swatch-grid"></div>
            </div>
          </div>

          <!-- Banner -->
          <div class="subpanel" data-sub="banner">
            <div class="card">
              <div class="card-title" data-i18n="settings.bannerTheme">Banner theme</div>
              <p class="muted small" data-i18n="settings.bannerTheme.hint">Stripe colours of the towed banner.</p>
              <div id="themes" class="swatch-grid"></div>
            </div>
          </div>

          <!-- Typo -->
          <div class="subpanel" data-sub="typo">
            <div class="card">
              <div class="card-title" data-i18n="settings.typography">Typography</div>
              <p class="muted small" data-i18n="settings.typography.hint">Banner font.</p>
              <div id="fonts" class="swatch-grid"></div>
            </div>
          </div>

          <!-- Sound -->
          <div class="subpanel" data-sub="sound">
            <div class="card">
              <div class="card-title" data-i18n="settings.sub.sound">Sound</div>
              <div class="switch-row">
                <div class="field-label"><span data-i18n="settings.engine.label">Engine sound</span><span class="hint" data-i18n="settings.engine.hint">Engine + sound while it flies</span></div>
                <label class="switch"><input id="sound" type="checkbox" /><span class="track"><span class="thumb"></span></span></label>
              </div>
              <div class="sep"></div>
              <div class="group-label"><span data-i18n="settings.flightSound.label">Flight sound</span><span class="hint" data-i18n="settings.flightSound.hint">Follows your animal — tap to preview</span></div>
              <div id="sounds" class="swatch-grid"></div>
            </div>
          </div>
        </section>

        <!-- CALENDAR -->
        <section class="panel" data-panel="calendar">
          <div class="panel-head"><h1 data-i18n="settings.tab.calendar">Calendar</h1><p class="muted" data-i18n="settings.calendar.subtitle">Read on this device only — never sent anywhere.</p></div>

          <!-- Provider picker -->
          <div id="cal-picker">
            <div class="provider-list">
              <button class="provider-btn" data-go="ical">
                <span class="provider-ico">
                  <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M10 13a5 5 0 0 0 7 0l3-3a5 5 0 0 0-7-7l-1.5 1.5"/><path d="M14 11a5 5 0 0 0-7 0l-3 3a5 5 0 0 0 7 7l1.5-1.5"/></svg>
                </span>
                <span class="provider-meta"><span class="provider-name"><span data-i18n="settings.provider.byLink">Add by link</span> <span class="reco" data-i18n="settings.badge.easiest">easiest</span></span><span id="pick-ical-status" class="provider-state">Not connected</span></span>
                <span class="chev">›</span>
              </button>

              <button class="provider-btn" data-go="icloud">
                <span class="provider-ico">
                  <svg viewBox="0 0 24 24" width="20" height="20" fill="currentColor"><path d="M16.4 1.4c0 1.1-.4 2.1-1.2 3-.9 1-2 1.6-3.1 1.5-.1-1.1.4-2.2 1.2-3C14.1 2 15.3 1.4 16.4 1.4zM20 17.2c-.5 1.2-.8 1.7-1.5 2.8-1 1.5-2.4 3.4-4.1 3.4-1.5 0-1.9-1-4-1-2 0-2.5 1-4 1-1.7 0-3-1.7-4-3.2-2.9-4.2-3.2-9.7-1-12.6C2.6 6.9 4.4 6 6.1 6c1.6 0 2.6 1 4 1 1.3 0 2.1-1 4-1 1.4 0 2.9.8 4 2.1-3.5 1.9-3 6.9.9 8.1z"/></svg>
                </span>
                <span class="provider-meta"><span class="provider-name">Apple iCloud</span><span id="pick-icloud-status" class="provider-state">Not connected</span></span>
                <span class="chev">›</span>
              </button>

              <button class="provider-btn" data-go="exchange">
                <span class="provider-ico">
                  <svg viewBox="0 0 24 24" width="20" height="20" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="4" y="3" width="16" height="18" rx="1"/><path d="M9 8h6M9 12h6M9 16h6"/></svg>
                </span>
                <span class="provider-meta"><span class="provider-name">Microsoft Exchange</span><span id="pick-exchange-status" class="provider-state">Not connected</span></span>
                <span class="chev">›</span>
              </button>

              <button class="provider-btn disabled" disabled>
                <span class="provider-ico">
                  <svg viewBox="0 0 48 48" width="22" height="22"><path fill="#FFC107" d="M43.6 20.5H42V20H24v8h11.3c-1.6 4.7-6.1 8-11.3 8-6.6 0-12-5.4-12-12s5.4-12 12-12c3.1 0 5.9 1.2 8 3.1l5.7-5.7C34.5 6.5 29.5 4.5 24 4.5 13.2 4.5 4.5 13.2 4.5 24S13.2 43.5 24 43.5 43.5 34.8 43.5 24c0-1.2-.1-2.3-.3-3.5z"/><path fill="#FF3D00" d="M6.3 14.7l6.6 4.8C14.7 16 19 13 24 13c3.1 0 5.9 1.2 8 3.1l5.7-5.7C34.5 6.5 29.5 4.5 24 4.5 16.3 4.5 9.7 8.9 6.3 14.7z"/><path fill="#4CAF50" d="M24 43.5c5.4 0 10.3-2 14-5.3l-6.5-5.5C29.6 34.5 26.9 35.5 24 35.5c-5.2 0-9.6-3.3-11.3-8l-6.6 5.1C9.5 39 16.2 43.5 24 43.5z"/><path fill="#1976D2" d="M43.6 20.5H42V20H24v8h11.3c-.8 2.2-2.2 4.2-4.1 5.6l6.5 5.5c-.5.4 7-5.1 7-14.6 0-1.2-.1-2.3-.4-3.5z"/></svg>
                </span>
                <span class="provider-meta"><span class="provider-name">Google Calendar</span><span class="provider-state" data-i18n="settings.provider.googleSoon">Sign-in coming soon</span></span>
                <span class="soon" data-i18n="settings.badge.soon">Soon</span>
              </button>
            </div>

            <div class="next-up">
              <div class="next-up-head">
                <span class="next-up-title">
                  <svg class="next-up-ico" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="9"/><path d="M12 7.5V12l3 2"/></svg>
                  <span data-i18n="settings.nextUp">Next up</span>
                </span>
                <button id="upcoming-refresh" class="icon-btn" title="Re-check your calendars now" aria-label="Refresh" data-i18n-title="settings.nextUp.refreshTitle">
                  <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-2.64-6.36"/><path d="M21 3v6h-6"/></svg>
                </button>
              </div>
              <ul id="upcoming" class="upcoming"><li class="up-empty" data-i18n="settings.upcoming.connect">Connect a calendar to see your meetings.</li></ul>
            </div>

            <p class="privacy" data-i18n="settings.privacy">🔒 Everything stays on this device. Quakpit has no server.</p>
          </div>

          <!-- iCal-link wizard -->
          <div id="wiz-ical" class="hidden">
            <button class="back" data-back data-i18n="settings.back">‹ Back</button>
            <h2 class="wiz-title" data-i18n="settings.ical.title">Add a calendar by link</h2>

            <div class="card">
              <div class="card-title" data-i18n="settings.ical.cardTitle">Paste a calendar link (iCal / .ics)</div>
              <ol class="steps-list">
                <li data-i18n-html="settings.ical.step1">Google Calendar (web): your calendar → <b>Settings and sharing</b> → <b>Integrate calendar</b> → copy <b>Secret address in iCal format</b>.</li>
                <li data-i18n-html="settings.ical.step2">Outlook / Fastmail / others: look for <b>Publish</b> or an <b>iCal/ICS</b> link.</li>
                <li data-i18n-html="settings.ical.step3">Paste it below — add as many calendars as you like.</li>
              </ol>
              <label class="stack"><span data-i18n="settings.ical.linkLabel">Calendar link</span><input id="ical-url" class="input" type="text" placeholder="https://…/basic.ics  (or webcal://…)" data-i18n-placeholder="settings.ical.urlPlaceholder" /></label>
              <label class="stack"><span data-i18n="settings.ical.nameLabel">Name (optional)</span><input id="ical-name" class="input" type="text" placeholder="Work" data-i18n-placeholder="settings.ical.namePlaceholder" /></label>
              <div class="row" style="margin-top:12px"><button id="ical-add" class="btn btn-primary" data-i18n="settings.ical.add">Add calendar</button></div>
              <p id="ical-error" class="warn hidden" style="margin-top:8px"></p>
            </div>

            <div class="card">
              <div class="card-title" data-i18n="settings.ical.feedsTitle">Your calendar links</div>
              <ul id="ical-feeds" class="feed-list"><li class="muted" data-i18n="settings.ical.none">None yet.</li></ul>
            </div>
          </div>

          <!-- iCloud wizard -->
          <div id="wiz-icloud" class="hidden">
            <button class="back" data-back data-i18n="settings.back">‹ Back</button>
            <h2 class="wiz-title" data-i18n="settings.icloud.title">Connect Apple iCloud</h2>

            <div id="i-step-form" class="card">
              <div class="card-title" data-i18n="settings.icloud.cardTitle">Connect with an app-specific password</div>
              <ol class="steps-list">
                <li data-i18n-html="settings.icloud.step1">Open <a href="https://appleid.apple.com" target="_blank" rel="noopener">appleid.apple.com</a> → Sign-In &amp; Security → <b>App-Specific Passwords</b> → generate one.</li>
                <li data-i18n="settings.icloud.step2">Enter your Apple ID and that password below.</li>
              </ol>
              <label class="stack"><span data-i18n="settings.icloud.appleId">Apple ID</span><input id="i-user" class="input" type="email" placeholder="you@icloud.com" /></label>
              <label class="stack"><span data-i18n="settings.icloud.appPassword">App-specific password</span><input id="i-pass" class="input" type="password" placeholder="xxxx-xxxx-xxxx-xxxx" /></label>
              <div class="row" style="margin-top:12px"><button id="i-connect" class="btn btn-primary" data-i18n="settings.action.connect">Connect</button></div>
            </div>

            <div id="i-step-connected" class="card hidden">
              <p class="ok"><span data-i18n="settings.state.connectedOk">✅ Connected</span> · <span id="i-detail"></span></p>
              <button id="i-disconnect" class="btn btn-outline" data-i18n="settings.action.disconnect">Disconnect</button>
            </div>

            <p id="i-error" class="warn hidden"></p>
          </div>

          <!-- Exchange wizard -->
          <div id="wiz-exchange" class="hidden">
            <button class="back" data-back data-i18n="settings.back">‹ Back</button>
            <h2 class="wiz-title" data-i18n="settings.exchange.title">Connect Microsoft Exchange</h2>

            <div id="x-step-form" class="card">
              <div class="card-title" data-i18n="settings.exchange.cardTitle">Connect to your company Exchange server</div>
              <ol class="steps-list">
                <li data-i18n-html="settings.exchange.step1">Ask IT for the EWS address — usually <b>https://mail.company.com/EWS/Exchange.asmx</b>.</li>
                <li data-i18n-html="settings.exchange.step2">Sign in with your Windows login, e.g. <b>CORP\jane</b> or <b>jane@corp.com</b>.</li>
                <li data-i18n-html="settings.exchange.step3">The server must be reachable — from the office network or VPN.</li>
              </ol>
              <label class="stack"><span data-i18n="settings.exchange.server">Server address</span><input id="x-server" class="input" type="text" placeholder="mail.company.com" data-i18n-placeholder="settings.exchange.serverPlaceholder" /></label>
              <label class="stack"><span data-i18n="settings.exchange.username">Username</span><input id="x-user" class="input" type="text" placeholder="CORP\jane" data-i18n-placeholder="settings.exchange.userPlaceholder" /></label>
              <label class="stack"><span data-i18n="settings.exchange.password">Password</span><input id="x-pass" class="input" type="password" /></label>
              <div class="row" style="margin-top:12px"><button id="x-connect" class="btn btn-primary" data-i18n="settings.action.connect">Connect</button></div>
            </div>

            <div id="x-step-connected" class="card hidden">
              <p class="ok"><span data-i18n="settings.state.connectedOk">✅ Connected</span> · <span id="x-detail"></span></p>
              <button id="x-disconnect" class="btn btn-outline" data-i18n="settings.action.disconnect">Disconnect</button>
            </div>

            <p id="x-error" class="warn hidden"></p>
          </div>
        </section>
      </main>
    </div>

    <script type="module" src="./settings.ts"></script>
  </body>
</html>
```

**Замечания к разметке:** токен-кнопки `title`/`minutes` и варианты скорости `×1/×1.5/×2` намеренно не переводятся (это литеральные токены шаблона и универсальные обозначения); плейсхолдеры-примеры значений (`you@icloud.com`, `xxxx-…`) не переводятся, а переводимые плейсхолдеры подключены через `data-i18n-placeholder`. Дубль кнопки iCloud удалён (см. выше).

- [ ] **Step 3: Modify `src/renderer/settings/settings.ts` — правки E1–E10**

**E1 — импорты** (после строки `import logoUrl from '../logo.png'`):

```ts
import { createI18n, resolveLocale, type I18n } from '../../shared/i18n'
import { applyI18n } from './i18n-apply'
```

**E2 — связанный переводчик** (после `const q = window.quakpit`):

```ts
// Bound translator; replaced by applyLocale() when the language changes.
let i18n: I18n = createI18n('en')
```

**E3 — ссылка на селект языка** (после `const displaySel = ...`):

```ts
const langSel = $<HTMLSelectElement>('lang')
```

**E4 — fallback-объект prefs** (строки 112–129): добавить поле `lang: 'auto',` после `customFlierName: ''`.

**E5 — fillPrefs** (после `template.value = p.messageTemplate`):

```ts
  langSel.value = p.lang
```

**E6 — имена опций из каталога** (5 мест):
- `renderHeads`: `name: h.name,` → `name: i18n.t(`option.head.${h.id}`),`
- `renderColors`: `name: c.name,` → `name: i18n.t(`option.color.${c.id}`),`
- `renderSounds`: `name.textContent = s.name` → `name.textContent = i18n.t(`option.sound.${s.id}`)`
- `renderThemes`: `name.textContent = t.name` → `name.textContent = i18n.t(`option.theme.${t.id}`)`
- `renderFonts`: `fill.textContent = 'Hello'` → `fill.textContent = i18n.t('option.fontPreview')` и `name.textContent = f.name` → `name.textContent = i18n.t(`option.font.${f.id}`)`

**E7 — статусы провайдеров в `renderCalendar`** (строки 355–368):

```ts
  pickIcalStatus.textContent = ic?.connected
    ? (ic.detail ?? i18n.t('settings.state.connected'))
    : i18n.t('settings.state.notConnected')
  pickIcalStatus.classList.toggle('connected', !!ic?.connected)
  pickIcloudStatus.textContent = i?.connected
    ? i.detail
      ? i18n.t('settings.state.connectedDetail', { detail: i.detail })
      : i18n.t('settings.state.connected')
    : i18n.t('settings.state.notConnected')
  pickIcloudStatus.classList.toggle('connected', !!i?.connected)
  pickExchangeStatus.textContent = x?.connected
    ? x.detail
      ? i18n.t('settings.state.connectedDetail', { detail: x.detail })
      : i18n.t('settings.state.connected')
    : i18n.t('settings.state.notConnected')
  pickExchangeStatus.classList.toggle('connected', !!x?.connected)
```

**E8 — пустые списки** (2 места):
- `renderUpcoming` (строки 396–398):
```ts
    li.textContent = connected
      ? i18n.t('settings.upcoming.empty')
      : i18n.t('settings.upcoming.connect')
```
- `renderFeeds` (строка 425): `li.textContent = i18n.t('settings.ical.none')`; кнопка удаления (строка 435): `rm.textContent = i18n.t('settings.action.remove')`.

**E9 — кнопки процессов** (3 места):
- iCal (строки 492/503): `icalAddBtn.textContent = i18n.t('settings.ical.adding')` и `= i18n.t('settings.ical.add')`
- iCloud (строки 511/521): `iConnect.textContent = i18n.t('settings.action.connecting')` и `= i18n.t('settings.action.connect')`
- Exchange (строки 533/549): `xConnect.textContent = i18n.t('settings.action.connecting')` и `= i18n.t('settings.action.connect')`

**E10 — блок языка перед секцией Init** (вставить перед `// ---- Init`):

```ts
// ---- Language -------------------------------------------------------------
/** Re-binds the translator and re-renders every localized surface. */
async function applyLocale(): Promise<void> {
  i18n = createI18n(resolveLocale(prefs.lang, navigator.language))
  document.documentElement.lang = i18n.locale
  document.title = i18n.t('settings.windowTitle')
  applyI18n(document, i18n)
  renderHeads()
  renderColors()
  renderThemes()
  renderFonts()
  renderSounds()
  renderPreview()
  await refreshCalendar()
  if (!wizIcal.classList.contains('hidden')) renderFeeds(await q.icalList())
}

langSel.addEventListener('change', async () => {
  prefs = await q.setPrefs({ lang: langSel.value as Prefs['lang'] })
  await applyLocale()
})
```

**E11 — init** (строки 565–568):

```ts
void (async () => {
  fillPrefs(await q.getPrefs())
  await applyLocale()
})()
```

- [ ] **Step 4: Type-check + build renderer**

Run: `npx tsc --noEmit && npm run build`
Expected: 0 ошибок типизации; сборка проходит (renderer импортирует `../../shared/i18n` за пределами root — Vite это разрешает).

- [ ] **Step 5: Commit**

```bash
git add src/renderer/settings/
git commit -m "feat(settings): data-i18n markup, applyI18n, language switcher; drop duplicate iCloud entry"
```

---

### Task 7: Русский README, описание пакета, AGENTS.md

**Files:**
- Modify: `README.md` (полная замена)
- Modify: `package.json:4` (description — двуязычная строка)
- Modify: `AGENTS.md` (правило языка UI)

**Interfaces:**
- Consumes: факты из git-истории форка (`dce004b`, `0265c35`, `8456bbc`) и Задачи 1–6.
- Produces: ничего исполняемого.

- [ ] **Step 1: Replace `README.md` целиком**

````markdown
<p align="center">
  <img src="src/renderer/logo.png" alt="Quakpit" width="120" />
</p>

<h1 align="center">Quakpit 🦆✈️</h1>

<p align="center">
  Маленькая утка на самолёте пролетает по всему экрану поверх всех окон и тащит за собой баннер,<br/>
  который напоминает о ближайшей встрече — со звуком авиамотора. Полностью автоматически, по вашему календарю.
</p>

<p align="center">
  <strong>Бесплатно и с открытым кодом.</strong> macOS · интерфейс на русском и английском.<br/>
  Форк оригинального <a href="https://ooble.studio"><strong>Quakpit</strong></a> от <strong>Ooble Studio</strong> 🐣
</p>

<p align="center">
  <img alt="License: MIT" src="https://img.shields.io/badge/license-MIT-1a1a1a" />
  <img alt="Platform: macOS" src="https://img.shields.io/badge/platform-macOS-1a1a1a" />
  <a href="https://x.com/ooblestudio"><img alt="Follow @ooblestudio on X" src="https://img.shields.io/badge/X-%40ooblestudio-1a1a1a" /></a>
</p>

---

## 🍴 Чем этот форк отличается от оригинала

Оригинальный Quakpit создан [Ooble Studio](https://ooble.studio). В этом форке добавлено и исправлено:

- ✈️ **Провайдер Microsoft Exchange (on-premises) по EWS** — корпоративный календарь через SOAP/EWS:
  Basic-аутентификация с автовосходящим переходом на NTLMv2, только HTTPS;
- 🆓 **Pro и лицензирование полностью удалены** — всё, что раньше было платным (персонажи, цвета
  самолёта, звуки, темы баннера, скорости полёта), теперь бесплатно и в комплекте; из кода убраны
  проверки лицензии и обращения к Polar;
- 🙈 **Режим «скрыть значок из Dock»** — приложение живёт только в строке меню (без иконки в Dock
  и Cmd+Tab), плюс новая иконка трея, сгенерированная из артворка приложения;
- 🐞 **Исправлен дублирующийся пункт «Apple iCloud»** в списке календарных провайдеров;
- 🌍 **Двуязычный интерфейс: русский и английский** — язык системы определяется автоматически,
  переключатель в «Настройки → Основные → Язык» (это изменение).

## Что оно делает

Quakpit тихо живёт в **строке меню**, следит за вашим календарём и за несколько минут до встречи
запускает утку в полёт **поверх всех** открытых окон — с баннером вроде _«Звонок с Джеком через
5 минут»_, гулом мотора и кряканьем. Всё. Никаких окон, никаких уведомлений, которые надо закрывать.

- **Тестовый полёт** в любой момент — или горячая клавиша **⌘⇧D**
- Работает с **Google Календарём**, **Apple iCloud**, **Microsoft Exchange (EWS)** и **iCal-ссылками** (.ics)
- Настраиваются время до напоминания, текст баннера, звук

## Приватность — ноль данных

У Quakpit **нет сервера** и **нет базы данных**. Приложение общается с Google / Apple / вашим
Exchange **напрямую** с вашего компьютера. События календаря хранятся **только в памяти** и никогда
не пишутся на диск. Локально могут сохраняться только токен Google и учётные данные iCloud/Exchange —
**зашифрованные ОС** (Keychain). Никакой телеметрии и аналитики. Никогда.

## Скачивание

Забирайте последнюю сборку для **macOS** со страницы [**Releases**](../../releases).

## Разработка

```bash
npm install
npm run dev      # запускайте из СВОЕГО терминала (не из автоматизированного шелла)
npm run build    # production-сборка в ./out
npm test         # vitest (локально: CI гоняет только tsc + build)
```

> Внимание: если шелл выставляет `ELECTRON_RUN_AS_NODE=1`, Electron не откроет окно. Используйте
> обычный терминал или `env -u ELECTRON_RUN_AS_NODE npm run dev`.

### Подключение Google Календаря (разовая настройка)

Нужен собственный Google OAuth-клиент (чтобы ничего не шло через чужие серверы):

1. <https://console.cloud.google.com/> → создать проект.
2. **APIs & Services → Library** → включить **Google Calendar API**.
3. **OAuth consent screen** → External. Добавить скоуп `.../auth/calendar.events.readonly`. В тестовом
   режиме добавьте свой аккаунт в **Test users** (верификация не нужна до 100 тестеров).
4. **Credentials → Create credentials → OAuth client ID → Desktop app**.
5. Скопировать `oauth-credentials.example.json` в **`oauth-credentials.json`** и вставить свой
   `clientId` / `clientSecret`. (Для desktop-приложений секрет не конфиденциален; файл в gitignore.)

Затем: **Настройки → Календарь → Google Календарь → Подключить**.

### Подключение iCloud (без облачных консолей)

iCloud работает по **CalDAV** с **паролем приложения**:

1. <https://appleid.apple.com> → **Вход и безопасность → Пароли приложений** → создать пароль.
2. В Quakpit: **Настройки → Календарь → Apple iCloud** → Apple ID + этот пароль → **Подключить**.

Пароль хранится **зашифрованным на вашем устройстве** (Keychain) и используется только для чтения
календарей по HTTPS напрямую у Apple.

### Подключение Microsoft Exchange (корпоративный)

Нужен EWS-адрес вашего сервера:

1. Узнайте у IT адрес EWS — обычно `https://mail.company.com/EWS/Exchange.asmx`.
2. В Quakpit: **Настройки → Календарь → Microsoft Exchange** → адрес, логин Windows (например,
   `CORP\jane`) и пароль → **Подключить**.

Сервер должен быть доступен из офисной сети или через VPN. Пароль хранится **зашифрованным на вашем
устройстве** и уходит только на ваш сервер Exchange.

### Подключение по ссылке (iCal / .ics)

**Настройки → Календарь → Добавить по ссылке**: вставьте ссылку вида
`https://…/basic.ics` (Google: _Настройки и доступ → Интеграция календаря → Секретный адрес в формате
iCal_) — и всё.

### Сборка macOS-приложения

```bash
npm run dist:mac   # -> dist/Quakpit-<version>-universal.dmg
```

Для локальной тестовой сборки без подписи: `npm run build && CSC_IDENTITY_AUTO_DISCOVERY=false npx electron-builder --dir`.
Подписанная и нотаризованная сборка (без предупреждений + автообновление) требует Apple Developer
аккаунт ($99/год) и сертификат _Developer ID Application_; задайте `APPLE_ID` /
`APPLE_APP_SPECIFIC_PASSWORD` / `APPLE_TEAM_ID` и `mac.notarize: true` в `electron-builder.yml`.

## Структура проекта

```
src/main/         Electron main (окна, трей, календари, планировщик, хранилище)
src/main/i18n.ts  локализация main-процесса
src/preload/      Безопасный мост для рендереров
src/renderer/     overlay (полёт) + settings (настройки)
src/shared/i18n/  Каталоги строк интерфейса (en + ru) и ядро i18n
scripts/          генераторы иконок трея и приложения
site/             статический лендинг (не входит в сборку приложения)
```

## Язык интерфейса

- По умолчанию — язык системы: русский → русский интерфейс, остальные → английский.
- Смена: **Настройки → Основные → Язык** — применяется сразу, без перезапуска.
- Добавить свой язык: скопируйте `src/shared/i18n/en.ts`, переведите значения и подключите новую
  локаль в `src/shared/i18n/core.ts` (`resolveLocale` + `pluralIndex`). Тест `catalogs.test.ts`
  проверит полноту каталога.

## Лицензия

[MIT](LICENSE) © оригинал — [Ooble Studio](https://ooble.studio). Этот форк распространяется
на тех же условиях.

<p align="center"><sub>Оригинальный Quakpit сделан с 🧡 командой <a href="https://ooble.studio">Ooble Studio</a></sub></p>
````

- [ ] **Step 2: `package.json` — двуязычное описание**

Заменить строку 4:

```json
  "description": "Утка на самолёте пролетает по экрану с баннером-напоминанием о встрече. A little duck in a plane flies across your screen towing a banner that reminds you of your next meeting.",
```

- [ ] **Step 3: `AGENTS.md` — обновить правило языка**

Найти предложение (первый абзац описания проекта): `UI and code comments are English.` и заменить на:

```
UI is bilingual (English + Russian) via `src/shared/i18n/` — every user-visible string comes from the catalogs (en.ts/ru.ts), never hardcoded; code comments are English.
```

- [ ] **Step 4: Type-check + tests**

Run: `npx tsc --noEmit && npm test`
Expected: зелёные.

- [ ] **Step 5: Commit**

```bash
git add README.md package.json AGENTS.md
git commit -m "docs: russian README with fork changelog, bilingual package description, i18n note in AGENTS"
```

---

### Task 8: Полная проверка и ручной smoke

**Files:** без изменений кода (фиксы, если что-то нашлось).

- [ ] **Step 1: Полный набор проверок**

Run: `npx tsc --noEmit && npm test && npm run build`
Expected: 0 ошибок, все тесты зелёные, сборка успешна. Это то, что гоняет CI — локально обязано проходить целиком.

- [ ] **Step 2: Ручной smoke (запускает пользователь из своего терминала)**

`env -u ELECTRON_RUN_AS_NODE npm run dev` — и проверить чек-лист:

1. Окно настроек открылось на русском (система с ru-локалью) — сайдбар, все три вкладки, визарды.
2. «Основные → Язык» → English — весь интерфейс мгновенно переключается (включая статические тексты, свотчи, заголовок окна).
3. После смены языка меню трея локализовано («Открыть Quakpit» / «Open Quakpit»).
4. «Отправить тестовый полёт» — баннер летит с русской фразой «Звонок с Джеком через 5 минут» (в ru) / англ. (в en).
5. Подключение iCloud с неверным паролем — ошибка в визарде на текущем языке.
6. Добавление iCal-ссылки: пустой список → «Пока ничего нет.», после добавления двух ссылок статус «2 календаря» (не «2 календарей»).
7. Полёт по реальной встрече (flyAtStart) — сообщение «{название} начинается сейчас».
8. Существующий prefs.json со старым англ. messageTemplate — поле «Текст баннера» НЕ перезаписано.

- [ ] **Step 3: Финальный коммит (если были фиксы)**

```bash
git add -A
git commit -m "fix(i18n): smoke-test fixes"
```

## Известные ограничения (зафиксированы спекой)

- Фон DMG-установщика (`build/dmg-background.png`) содержит впаянный английский текст — статичная картинка, перерисовка вне этой задачи.
- Текст `MessageText` от Exchange-сервера Microsoft приходит на языке сервера и не переводится.
- `site/` (лендинг) не переводится — не входит в сборку приложения.
- Дефолтный шаблон баннера у существующих пользователей остаётся прежним (намеренно).







